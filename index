<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>NeoOS 26 Caracals</title>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

<style>
:root {
  --bg: #0b0b0f;
  --card: rgba(255,255,255,0.08);
  --blur: blur(20px);
  --radius: 28px;
  --text: #ffffff;
  --sub: #b5b5c0;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  font-family: 'Inter', sans-serif;
}

body {
  background: radial-gradient(circle at top, #1c1c28, #0b0b0f);
  color: var(--text);
}

/* HERO */
.hero {
  min-height: 100vh;
  padding: 48px 24px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
}

.hero h1 {
  font-size: clamp(2.8rem, 6vw, 4.5rem);
  font-weight: 700;
  letter-spacing: -0.03em;
}

.hero span {
  background: linear-gradient(135deg, #7cf7ff, #9d8cff);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.hero p {
  max-width: 520px;
  margin-top: 16px;
  font-size: 1.05rem;
  color: var(--sub);
}

/* BUTTON */
.hero a {
  margin-top: 32px;
  padding: 14px 34px;
  border-radius: 999px;
  background: linear-gradient(135deg, #7cf7ff, #9d8cff);
  color: #000;
  font-weight: 600;
  text-decoration: none;
}

/* CARDS */
.section {
  padding: 64px 24px;
}

.grid {
  max-width: 1100px;
  margin: auto;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 24px;
}

.card {
  background: var(--card);
  backdrop-filter: var(--blur);
  border-radius: var(--radius);
  padding: 28px;
  border: 1px solid rgba(255,255,255,0.08);
}

.card h3 {
  font-size: 1.25rem;
  margin-bottom: 10px;
}

.card p {
  color: var(--sub);
  font-size: 0.95rem;
}

/* FOOTER */
footer {
  padding: 32px;
  text-align: center;
  color: var(--sub);
  font-size: 0.85rem;
}
</style>
</head>

<body>

<section class="hero">
  <h1>NeoOS 26 <span>Caracals</span></h1>
  <p>
    A refined, elegant Linux experience inspired by clarity, speed, and modern design.
    Built for creators, explorers, and everyday users.
  </p>
  <a href="#">Download (Coming Soon)</a>
</section>

<section class="section">
  <div class="grid">
    <div class="card">
      <h3>Minimal Design</h3>
      <p>Clean layouts, soft motion, and a distraction-free desktop inspired by premium OS design.</p>
    </div>

    <div class="card">
      <h3>Fast & Fluid</h3>
      <p>Optimized KDE experience with smooth animations and lightweight performance.</p>
    </div>

    <div class="card">
      <h3>Open & Free</h3>
      <p>Built on open-source foundations with full transparency and community spirit.</p>
    </div>
  </div>
</section>

<footer>
  © 2026 NeoOS Project · Built on open-source software
</footer>

</body>
</html>
