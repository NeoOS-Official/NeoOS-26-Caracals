
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>NeoOS 26 — Official Site</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', 'Roboto', sans-serif;
      background: #ffffff;
      color: #1d1d1f;
    }

    header {
      background: #000;
      color: #fff;
      padding: 60px 20px;
      text-align: center;
    }

    header h1 {
      font-size: 48px;
      margin-bottom: 10px;
    }

    header p {
      font-size: 20px;
      color: #ccc;
    }

    .nav {
      display: flex;
      justify-content: center;
      gap: 30px;
      background: #f5f5f7;
      padding: 14px 0;
      font-size: 14px;
    }

    .nav a {
      text-decoration: none;
      color: #1d1d1f;
      font-weight: 500;
    }

    .section {
      padding: 60px 20px;
      max-width: 1100px;
      margin: auto;
    }

    .section h2 {
      font-size: 32px;
      margin-bottom: 20px;
      text-align: center;
    }

    .section p {
      font-size: 18px;
      color: #6e6e73;
      text-align: center;
      max-width: 700px;
      margin: auto;
    }

    .features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 24px;
      margin-top: 40px;
    }

    .feature {
      background: #f9f9fa;
      border-radius: 16px;
      padding: 24px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.06);
      transition: transform 0.3s ease;
    }

    .feature:hover {
      transform: translateY(-4px);
    }

    .feature h3 {
      font-size: 20px;
      margin-bottom: 10px;
    }

    .feature p {
      font-size: 15px;
      color: #6e6e73;
    }

    .download {
      background: #000;
      color: #fff;
      text-align
